services:
  datafusion:
    build:
      context: ./docker
      dockerfile: ./Dockerfile 
    container_name: datafusion
    stdin_open: true   
    tty: true    
    volumes:
      - ./docker/shared:/workspace/shared
    networks:
      - standard

networks:
  standard:
    driver: bridge